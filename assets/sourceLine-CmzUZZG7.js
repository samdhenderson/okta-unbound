import{i as l,a as h}from"./membershipAnalysis-CAnarCAG.js";const c={exact:"Added by Rule:",inferred:"Added by rule:",ambiguous:"Rule:"};function b(e){return e.detail?`${e.caption} ${e.detail}`:e.caption}function p(e,t){return e?t?"Not every rule condition could be evaluated against this user. The rules listed are the source of this membership; Okta does not record which rule added a member.":"Every rule listed provably matches this user. Okta does not record which rule added a member, so this is the classifier evaluating rule conditions, not an Okta assertion.":"The classifier could not resolve which rule granted this membership, so everything listed is a candidate rather than the answer, and none of them is credited."}function m(e){if(e.rules.length===0)return{caption:"Okta confirms: added directly",detail:"",description:"Okta's own record for this user and group names no rule managing the membership, so it was added by hand. This is Okta answering rather than the classifier evaluating rule conditions, and it is the answer where the two differ.",proven:!0};const t=e.rules.length>1?` (${e.rules.length} rules)`:"";return{caption:"Okta confirms: added by rule:",detail:`${e.rules.map(r=>r.name).join(", ")}${t}`,description:"Okta's own record for this user and group names the rule(s) managing the membership. This is Okta answering rather than the classifier evaluating rule conditions, and it is the answer where the two differ.",proven:!0}}function f(e){const{membershipType:t,rules:r,attribution:n,group:o,provenance:a}=e;if(a)return m(a);const i=l(n);if(t==="UNKNOWN")return{caption:"Source not determined",detail:"",description:"This membership was never classified — the group rules it would be checked against could not be loaded. It is not a manual add and not a rule grant; the answer is missing.",proven:!1};if(t==="DIRECT")return{caption:"Added directly",detail:"",description:i?"No rule was matched, though not every rule condition could be evaluated. The user was added to the group by hand.":"No active group rule explains this membership, so the user was added to the group by hand.",proven:!i};if(r.length===0)return o.type==="APP_GROUP"?{caption:"Managed by app",detail:"",description:"This group is mastered by an application, which manages its own members. No group rule explains the membership.",proven:!i}:{caption:"Rule-managed, rule not identified",detail:"",description:"The membership is rule-managed, but no rule is attributed to it, so the granting rule cannot be named here.",proven:!1};const s=h(n),d=r.length>1?` (${r.length} ${s?"rules":"candidates, unresolved"})`:"";return{caption:c[n],detail:`${r.map(u=>u.name).join(", ")}${d}`,description:p(s,i),proven:!i}}export{f as m,b as s};
